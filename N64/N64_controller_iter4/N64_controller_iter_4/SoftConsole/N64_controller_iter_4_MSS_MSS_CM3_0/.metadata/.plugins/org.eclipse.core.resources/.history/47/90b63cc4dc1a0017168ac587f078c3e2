#include "drivers/mss_uart/mss_uart.h"
#include "inttypes.h"

//#define CONTROLLER_DATA_REG ((uint8_t *) FPGA_FABRIC_BASE)
#define CONTROLLER_DATA_REG ((uint32_t *) 0x40050000)

int main(void)
{
	while( 1 )
	{
		printf("Hello World\n");

		//uint8_t tx_buff[4];
		uint32_t tx_buff;
		uint8_t tx_msg_length;
		int i;

//		tx_msg_length = 4;
//		for(i = 0; i < tx_msg_length; i++){
//			tx_buff[i] = *(CONTROLLER_DATA_REG + i);
//		}

		tx_buff = *CONTROLLER_DATA_REG;

		printf("Controller Data: %" PRIu32 "\n",tx_buff);

//		printf("Controller Data Byte 1: %ul\n", (unsigned)tx_buff[0]);
//		printf("Controller Data Byte 2: %ul\n", (unsigned)tx_buff[1]);
//		printf("Controller Data Byte 3: %ul\n", (unsigned)tx_buff[2]);
//		printf("Controller Data Byte 4: %ul\n", (unsigned)tx_buff[3]);
	}

	return 0;
}
